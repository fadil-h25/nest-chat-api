import { PrismaHttpKnownFilter } from './prisma-http-known.filter';

describe('PrismaHttpKnownFilter', () => {
  it('should be defined', () => {
    expect(new PrismaHttpKnownFilter()).toBeDefined();
  });
});
