import { PrismaWsKnownFilter } from './prisma-ws-known.filter';

describe('PrismaWsKnownFilter', () => {
  it('should be defined', () => {
    expect(new PrismaWsKnownFilter()).toBeDefined();
  });
});
